const basicReducer = (state = {}, action = {}) => {
    switch (action.type) {
        case 'BASIC':
            return {};
        default:
            return {};
    }
};

export default basicReducer;